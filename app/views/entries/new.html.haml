%main.entry
  = form_for @entry do |f|
    - if @entry.errors.any?
      .errors
        - @entry.errors.full_messages.each do |msg|
          %p
            = msg
    .field
      = f.label :name, "Enter a name:"
      = f.text_field :name
    .field
      = f.submit
